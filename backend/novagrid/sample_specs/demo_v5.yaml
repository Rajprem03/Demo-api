openapi: 3.0.0
info:
  title: NovaGrid Demo API
  version: 5.0.0
paths:
  /pets:
    get:
      responses:
        "200":
          description: OK
  /pets/{id}:
    get:
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: intger
      responses:
        "200":
          description: OK